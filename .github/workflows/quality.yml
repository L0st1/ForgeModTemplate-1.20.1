name: Code quality

on:
  pull_request:
    types: [opened, reopened, synchronize]
  push:
    branches: [master]

permissions:
  contents: read

jobs:
  quality:
    name: Format and static analysis
    runs-on: ubuntu-latest
    env:
      # 模板默认代理指向开发者本机；托管 runner 不存在该代理。
      MC_MOD_PROXY_ENABLED: 'false'
    steps:
      - name: Check out source
        uses: actions/checkout@v6

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Run quality checks
        run: ./gradlew -b quality.gradle qualityCheck --no-daemon
