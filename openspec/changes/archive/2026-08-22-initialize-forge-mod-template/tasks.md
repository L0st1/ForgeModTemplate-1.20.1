## 1. 迁移准备与模板边界

- [x] 1.1 生成当前仓库文件清单，标记需要保留、通用化、替换和移除的内容，并确认模板不得携带的旧业务标识与个人路径
- [x] 1.2 在工作区内建立被 Git 忽略的可恢复迁移备份，包含待替换源码、资源、文档、脚本和第三方 JAR，并验证备份可读
- [x] 1.3 定义模板占位符、默认项目参数、初始化状态标记以及空白/示例模式和可选模块清单

## 2. Gradle 与元数据基线

- [x] 2.1 整理 `gradle.properties`，集中 Minecraft、Forge、Java、Gradle 和 Mod 项目身份属性，并移除具体业务身份与个人代理系统属性
- [x] 2.2 更新 `build.gradle` 的资源处理与 JAR 命名，使 `mods.toml`、`pack.mcmeta` 和产物元数据从规范化属性生成
- [x] 2.3 更新 `settings.gradle`，移除 Foojay resolver 强依赖，并在插件解析前加载网络配置
- [x] 2.4 新增受版本控制的代理默认配置，默认启用 `127.0.0.1:7890`，实现本地覆盖、环境覆盖和禁用语义
- [x] 2.5 更新 `.gitignore`，忽略本地代理覆盖、迁移备份、Gradle/IDE 缓存、构建产物、代码图数据库和个人部署配置

## 3. 安全的模板源码与资源

- [x] 3.1 用参数化的最小公共 Mod 入口替换 `health_control` 源码，确保公共入口不引用客户端专属类
- [x] 3.2 建立独立客户端包和 Dist 限定加载结构，并添加不改变服务端权威状态的安全示例模式
- [x] 3.3 参数化资源命名空间、语言文件、`mods.toml` 和 `pack.mcmeta`，确保模板状态与初始化后的项目均无未解析元数据
- [x] 3.4 创建 Mixin 可选模块，包含构建配置、Mixin 配置文件和最小示例，并确保未选择时不残留相关依赖
- [x] 3.5 创建 Forge 配置可选模块，包含配置注册和最小可验证配置项
- [x] 3.6 创建网络通信可选模块，包含消息注册、方向校验、服务端处理和示例调用
- [x] 3.7 创建 GameTest 可选模块，包含必要运行配置和最小测试示例

## 4. PowerShell 初始化器

- [x] 4.1 实现交互式参数收集和等价的非交互参数入口，覆盖全部项目身份、初始化模式和模块选择
- [x] 4.2 实现 `mod_id`、Java 包名、主类名、版本、必填文本和目标状态验证，并为失败返回中文错误与非零退出码
- [x] 4.3 实现写入前文件冲突预检和可恢复暂存流程，保证验证或替换失败时不会留下部分初始化状态
- [x] 4.4 实现 Java/资源目录重命名、占位符替换、Gradle 属性更新和未选模块裁剪
- [x] 4.5 实现初始化完成标记、重复执行保护和显式重置保护
- [x] 4.6 实现初始化后自检，扫描未解析占位符、旧项目标识、非法路径和元数据不一致，并输出后续诊断与构建命令

## 5. 本地开发工作流

- [x] 5.1 实现 `scripts/doctor.ps1`，检查 Java 17、Wrapper、关键配置、代理状态和依赖解析前置条件，并提供中文修复建议
- [x] 5.2 通用化本地构建脚本，使其检测并执行已有格式化/静态检查任务、运行测试、构建 JAR 并准确报告产物或失败
- [x] 5.3 更新 VS Code 构建与诊断任务，确保只使用仓库相对路径和项目内 Wrapper
- [x] 5.4 审核并修正常用 Forge、Mixin、配置、网络和 GameTest 代码片段，移除个人 JDK、游戏实例和部署目录

## 6. 文档、Skill 与 OpenSpec

- [x] 6.1 用简体中文 `README.md` 替换默认 MDK README，说明模板定位、初始化、首次构建、代理覆盖和技术基线
- [x] 6.2 编写中文开发指南，覆盖公共端/客户端边界、可选模块、开发运行、构建产物和常见故障排查
- [x] 6.3 将 `forge-addon-mod` 重构为通用 `forge-mod-development` skill，更新触发范围、决策流程、引用资料和脚本路径
- [x] 6.4 更新 OpenSpec 项目上下文，记录 Forge 1.20.1、Java 17、简体中文文档和模板维护约定

## 7. 清理旧项目内容

- [x] 7.1 在确认迁移备份有效后，移除旧 `health_control` 业务文件、飞机 Mod JAR 和飞机补丁专用引用
- [x] 7.2 将仍有通用价值的开发经验改写到模板文档后，移除重复、过时或领域专用的开发记录
- [x] 7.3 清理 `build/`、`bin/`、`.gradle/` 和本地代码图数据库等生成物，并确认其均被忽略
- [x] 7.4 全仓扫描 `health_control`、`plane-speed-patch`、第三方飞机 Mod 名称、DeceasedCraft 路径和个人 JDK 路径，确认模板交付内容不存在残留

## 8. 自动化验证与验收

- [x] 8.1 为初始化器增加脚本级测试，覆盖有效参数、非法参数、重复初始化、替换一致性和失败回滚
- [x] 8.2 在临时副本中生成最小空白项目并执行无缓存干净构建，核对 JAR 文件名和内部 Mod 元数据
- [x] 8.3 在临时副本中生成全模块示例项目并执行格式检查、GameTest 编译和干净构建
- [x] 8.4 验证默认代理、本地地址覆盖和禁用代理三种模式，确认诊断输出和 Gradle 解析行为符合规格
- [x] 8.5 对示例项目执行开发客户端启动冒烟检查和专用服务器类加载检查，确认客户端类不会污染公共入口
- [x] 8.6 在非模板作者绝对路径下验证 VS Code 任务和 Wrapper 构建，并扫描生成项目中是否存在个人路径
- [x] 8.7 运行 `openspec validate initialize-forge-mod-template --type change --strict`，修复全部规格格式或一致性错误并记录最终验证结果
